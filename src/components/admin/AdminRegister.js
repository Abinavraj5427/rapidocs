import React from "react";
import { useHistory } from "react-router-dom";

const AdminRegister = () => {
  const history = useHistory();
  return <h1>Admin Register</h1>;
};

export default AdminRegister;
