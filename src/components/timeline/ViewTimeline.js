import React, { useState } from 'react';
import Timeline from '../layout/Timeline';

const ViewTimeline = () => {
  return (
    <div>
      <Timeline uid={0} />
    </div>
  );
};

export default ViewTimeline;
